step 1  : npm init -y
step 2  : npm i -d typescript   /  npm install typescript --save-dev
step 3  : npx tsc --init
 then go to tsconfig.json and set "outDir": "./dist",
  "compilerOptions": {
    // File Layout
    "rootDir": "./src",
    "outDir": "./dist",
  }

then go to src/index.ts / create it  
and write some ts code simple greet function 

step 4  : npx tsc
step 5  : node ./dist/index.js



step 6  : npx tsc --watch     [dont need to run this again and again just save the file it will compile automatically]